<Window x:Class="Onyx.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:local="clr-namespace:Onyx"
        Style="{StaticResource OnyxWindow}"
        Title="Onyx" Icon="Assets/onyx.ico"
        Width="740" Height="820"
        MinWidth="640" MinHeight="500"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Height="40">
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Margin="0,0,8,0"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True">
                <Button Style="{StaticResource ChromeButton}" Click="Minimise" ToolTip="Minimise">
                    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Path Data="M 0,0 H 10"
                              Stroke="{DynamicResource TextSecondary}"
                              StrokeThickness="1.4"
                              StrokeStartLineCap="Round"
                              StrokeEndLineCap="Round"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center" />
                    </Grid>
                </Button>
                <Button Style="{StaticResource ChromeButton}" Click="CloseWindow" ToolTip="Close">
                    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Path Data="M 0,0 L 9,9 M 9,0 L 0,9"
                              Stroke="{DynamicResource TextSecondary}"
                              StrokeThickness="1.4"
                              StrokeStartLineCap="Round"
                              StrokeEndLineCap="Round"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center" />
                    </Grid>
                </Button>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Margin="24,6,24,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <Image Source="{StaticResource OnyxLogo}" Width="42" Height="48" Margin="0,0,12,0" />
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Onyx" Style="{StaticResource TitleText}" />
                        <TextBlock Text="Your RitoShark plugins, together." Style="{StaticResource CaptionText}" />
                    </StackPanel>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                    <TextBlock Text="{Binding LastChecked}" Style="{StaticResource CaptionText}" />
                    <TextBlock Text="  ·  " Style="{StaticResource CaptionText}">
                        <TextBlock.Visibility>
                            <Binding Path="StatusMessage" Converter="{StaticResource NotEmptyToVisibility}" />
                        </TextBlock.Visibility>
                    </TextBlock>
                    <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource CaptionText}" />
                </StackPanel>
            </StackPanel>

            <Button Grid.Column="1"
                    Style="{StaticResource PillButton}"
                    Content="Refresh plugins"
                    VerticalAlignment="Center"
                    Command="{Binding RefreshCommand}" />
        </Grid>

        <Grid Grid.Row="2">
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          Padding="24,0,16,20"
                          HorizontalScrollBarVisibility="Disabled">
                <ItemsControl ItemsSource="{Binding Groups}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,0,0,14">
                                <StackPanel Orientation="Horizontal" Margin="2,0,0,8">
                                    <Path Data="{Binding Icon}"
                                          Stroke="{DynamicResource Accent}"
                                          StrokeThickness="1.3"
                                          StrokeStartLineCap="Round"
                                          StrokeEndLineCap="Round"
                                          StrokeLineJoin="Round"
                                          Width="14" Height="14"
                                          Stretch="Uniform"
                                          VerticalAlignment="Center" />
                                    <TextBlock Text="{Binding Title}"
                                               Style="{StaticResource CaptionText}"
                                               Foreground="{DynamicResource TextSecondary}"
                                               FontWeight="SemiBold"
                                               Margin="8,0,0,0"
                                               VerticalAlignment="Center" />
                                </StackPanel>

                                <ItemsControl ItemsSource="{Binding Rows}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <local:PluginRow Margin="0,0,0,8" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

        </Grid>

        <Border Grid.Row="3" Background="{DynamicResource SurfaceRaised}"
                BorderBrush="{DynamicResource Separator}" BorderThickness="0,1,0,0"
                CornerRadius="0,0,10,10" Padding="24,0" Height="34">
            <Grid DataContext="{Binding AppUpdate}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding VersionLabel}" Style="{StaticResource CaptionText}"
                           VerticalAlignment="Center" ToolTip="Onyx version" />
                <TextBlock Grid.Column="1" Text="{Binding Status}" Style="{StaticResource CaptionText}"
                           Foreground="{DynamicResource TextMuted}" Margin="16,0"
                           TextTrimming="CharacterEllipsis" VerticalAlignment="Center"
                           ToolTip="{Binding Detail}" />
                <Button Grid.Column="2" Style="{StaticResource LinkButton}"
                        Content="{Binding ActionLabel}" Command="{Binding ActionCommand}"
                        ToolTip="{Binding Detail}" VerticalAlignment="Center" Padding="0" FontSize="12" />
            </Grid>
        </Border>

        <local:PluginDetail Grid.Row="0" Grid.RowSpan="4" />

        <local:ConfirmationSheet Grid.Row="0" Grid.RowSpan="4" />
    </Grid>
</Window>
